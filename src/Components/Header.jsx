import { SignedIn, SignedOut, SignInButton, UserButton, UserProfile, useUser } from '@clerk/clerk-react';
import Logo from './Logo';

const Header = () => {
  return (
    <header className='bg-blue-400'>
        Header
        </header>
  )
}

export default Header